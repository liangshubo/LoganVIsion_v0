import osfrom importlib import import_moduleimport torchimport torch.nn as nnfrom torch.autograd import Variable# from timm.models.layers import DropPath, to_2tuple, trunc_normal_#  新的timm 版本from timm.layers import DropPath, to_2tuple, trunc_normal_import warningswarnings.filterwarnings("ignore")global test_onlyfrom .base_model import BaseModel2class Model(BaseModel2):    def __init__(self, args, ckp):        super(Model, self).__init__(args, ckp)        self.model_define = args.model        print('\033[1;32m[ =======> Making {:} {:}<======= ]\033[0m '.format(self.model_savename, self.model_define))        self.load_model()    def set_model_savename(self):        model_save_name = "model"        return model_save_nameclass Model2(BaseModel2):    def __init__(self, args, ckp):        super(Model2, self).__init__(args, ckp)        self.model_define = args.model2        self.load_model()        print('\033[1;32m[ =======> Making {:} {:}<======= ]\033[0m '.format(self.model_savename, self.model_define))    def set_model_savename(self):        model_save_name = "model2"        return model_save_nameclass Model_control(BaseModel2):    def __init__(self, args, ckp):        super(Model_control, self).__init__(args, ckp)        self.model_define = args.model        print('\033[1;32m[ =======> Making {:} {:}<======= ]\033[0m '.format(self.model_savename, self.model_define))        self.load_model()    def set_model_savename(self):        model_save_name = "model"        return model_save_name    def forward(self, x, idx):        return self.model(x, idx)